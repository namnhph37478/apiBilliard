# ===================== App =====================
NODE_ENV=development
PORT=3000
APP_NAME=Billiard POS
TZ=Asia/Ho_Chi_Minh
BUILD_ID=local-dev

# ===================== Database =====================
# Mongo URI (chứa DB name)
MONGODB_URI=mongodb://127.0.0.1:27017/billiard
# Bật log mongoose (tùy chọn)
MONGODB_DEBUG=false

# ===================== Auth / JWT =====================
# Secret để ký JWT (bắt buộc đổi ở môi trường thật)
JWT_SECRET=please-change-me
# Issuer tuỳ chọn
JWT_ISSUER=apiBiliard
# Thời hạn token (ví dụ: 1h, 2h, 7d)
ACCESS_TOKEN_TTL=1h
REFRESH_TOKEN_TTL=7d

# Lưu JWT vào HttpOnly Cookie (nếu muốn)
AUTH_SET_COOKIE=false
COOKIE_DOMAIN=
COOKIE_SECURE=false
COOKIE_SAME_SITE=Lax
ACCESS_COOKIE_NAME=access_token
REFRESH_COOKIE_NAME=refresh_token

# ===================== CORS =====================
# Danh sách origin cho FE, phân tách bằng dấu phẩy. Dùng "*" để cho phép tất cả.
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# ===================== Upload =====================
# Thư mục lưu file upload (đã serve qua /uploads)
UPLOAD_DIR=public/uploads
# Giới hạn dung lượng upload (multer) — 5mb, 10mb...
UPLOAD_MAX_SIZE=10mb

# ===================== Reports / Export =====================
# Giới hạn số dòng khi xuất Excel
MAX_EXPORT_ROWS=10000

# ===================== Backup Job =====================
# Bật lịch backup mỗi ngày (đọc giờ từ Setting.backup.timeOfDay nếu không có BACKUP_CRON)
ENABLE_BACKUP_SCHEDULE=false
# Cron cho backup (nếu muốn ghi đè), ví dụ: "0 2 * * *" = 02:00 mỗi ngày
BACKUP_CRON=
# Số file backup tối đa cần giữ lại (0 = bỏ qua, chỉ theo retentionDays trong Setting)
BACKUP_MAX_FILES=0

# ===================== Logging =====================
# Mức log: trace | debug | info | warn | error | fatal
LOG_LEVEL=info


